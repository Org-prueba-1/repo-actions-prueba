plugins {
    id "org.sonarqube" version "4.3.1.3277"
}

sonarqube {
    properties {
        property "sonar.projectKey", "mi_proyecto"
        property "sonar.host.url", System.getenv("SONAR_HOST_URL")
        property "sonar.login", System.getenv("SONAR_TOKEN")
    }
}
